packages:
  - 'apps/*'
  - 'packages/*'

supportedArchitectures:
  os:
    - current
    - linux
    - darwin
  cpu:
    - current
    - x64
    - arm64

saveExact: true
progress: false

# 単位は分。7 日。Renovate 側の minimumReleaseAge と揃えている
minimumReleaseAge: 10080

allowBuilds:
  esbuild: true
  workerd: true
  # postinstall が Git フックを .git/hooks へ入れる。止めるとフックが有効にならない
  lefthook: true
  # drizzle-orm / better-auth の optional peer として入るだけで、
  # このリポジトリは libsql しか使わないためビルドは不要
  '@prisma/client': false
  better-sqlite3: false
  # どちらも postinstall は `astryx init` を促す 1 行を出すだけで、
  # ビルド成果物を作らない。走らせても得るものが無いので止める
  '@astryxdesign/core': false
  '@astryxdesign/cli': false
